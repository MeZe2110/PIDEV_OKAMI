{% extends 'Template_Back/base.html.twig' %}

{% block title %}Equipement{% endblock %}

{% block content %}
    <h1>Equipement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipement.id }}</td>
            </tr>
            <tr>
                <th>Nomeq</th>
                <td>{{ equipement.nomeq }}</td>
            </tr>
            <tr>
                <th>Etateq</th>
                <td>{{ equipement.etateq ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Dispoeq</th>
                <td>{{ equipement.dispoeq ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipement_index') }}">back to list</a>

    <a href="{{ path('app_equipement_edit', {'id': equipement.id}) }}">edit</a>

    {{ include('equipement/_delete_form.html.twig') }}
{% endblock %}
