{% extends 'Template_Back/base.html.twig' %}

{% block title %}Categories equipement index{% endblock %}



{% block content %}
    <h1>Categories equipement index</h1>

    <table class="table datatable">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nomcate</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categoriesequipement in categoriesequipements %}
            <tr>
                <td>{{ categoriesequipement.id }}</td>
                <td>{{ categoriesequipement.nomcate }}</td>
                <td>

                    <a href="{{ path('app_yes_edit', {'id': categoriesequipement.id}) }}">edit</a>
                    {{ include('yes/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_yes_new') }}">Create new</a>
{% endblock %}
