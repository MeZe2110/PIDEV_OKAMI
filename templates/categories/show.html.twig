{% extends 'base.html.twig' %}

{% block title %}Stockcategories{% endblock %}

{% block body %}
 
<div class="d-flex align-items-center justify-content-center" style="height: 550px;">
  <table class="center" style="width:50px">
    <tbody>
         <tr>
            <th>Id</th>
            </tr>
            <tr>
            <td>{{ stockcategory.id }}</td>
            </tr>
            <tr>
                <th>Typecat</th>
                </tr>
                <tr>
                <td>{{ stockcategory.typecat }}</td>
            </tr>
        </tbody>
    </table>
 <div class="mt-4 d-flex justify-content-center">
    <a href="{{path('app_categories_edit', {'id': stockcategory.id}) }}" class="btn btn-primary mx-3">Edit</a>
    {{ include('categories/_delete_form.html.twig') }}
    <a href="{{ path('app_categories_index') }}" class="btn btn-secondary mx-3">Back to List</a>
    
  </div>
</div>
{% endblock %}



