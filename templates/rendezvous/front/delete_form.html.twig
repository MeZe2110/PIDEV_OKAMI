<form method="post" action="{{ path('front_rendezvous_delete', {'id': r.id}) }}" onsubmit="return confirm('Are you sure you want to remove the Rendez-Vous?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ r.id) }}">
    <button class="btn">Supprimer</button>
</form>
