<form method="post" action="{{ path('back_rendezvous_type_delete', {'id': rendezvous_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendezvous_type.id) }}">
    <button class="btn">Supprimer</button>
</form>
