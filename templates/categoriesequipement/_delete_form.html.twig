<form method="post" action="{{ path('app_categoriesequipement_delete', {'id': categoriesequipement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categoriesequipement.id) }}">
    <button class="btn">Delete</button>
</form>
