{% extends 'base.html.twig' %}

{% block title %}Categoriesequipement index{% endblock %}

{% block body %}
    <h1>Categoriesequipement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nomcate</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categoriesequipement in categoriesequipements %}
            <tr>
                <td>{{ categoriesequipement.id }}</td>
                <td>{{ categoriesequipement.nomcate }}</td>
                <td>
                    <a href="{{ path('app_categoriesequipement_show', {'id': categoriesequipement.id}) }}">show</a>
                    <a href="{{ path('app_categoriesequipement_edit', {'id': categoriesequipement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categoriesequipement_new') }}">Create new</a>
{% endblock %}
