
{{form_start (form,{'attr': {'novalidate': 'novalidate '}} ) }}

<table>

<tr>
<td> {{ form_label(form.role, "role :  ") }} </td>
<td> {{ form_widget(form.role) }} </td>
<td> {{ form_errors(form.role) }} </td>
</tr>

</table>
 <div class="col-sm-10">
      <button type="submit" class="btn btn-primary" _msttexthash="499135" _msthash="115"{{ button_label|default('Save') }}>create</button>
           

     </div>

                 
{{ form_end(form) }}
