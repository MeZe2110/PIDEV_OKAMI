{% extends 'base.html.twig' %}



{% block body %}

<div class="d-flex align-items-center justify-content-center" style="height: 550px;">
  <table class="center" style="width:50px">
    <tbody>
    <td><td>
      <tr>
        <th>Id</th>
        </tr>
        <tr>
        <td>{{ stock.id }}</td>
      </tr>
      <tr>
        <th>Nomst</th>
        </tr>
        <tr>
        <td>{{ stock.nomst }}</td>
      </tr>
      <tr>
        <th>Quantites</th>
        </tr>
        <tr>
        <td>{{ stock.quantites }}</td>
      </tr>
      <tr>
        <th>Dateexpirationst</th>
        </tr>
        <tr>
        <td>{{ stock.dateexpirationst ? stock.dateexpirationst|date('Y-m-d') : '' }}</td>
      </tr>
      <tr>
        <th>Description</th>
        </tr>
        <tr>
        <td>{{ stock.description }}</td>
      </tr>
      <tr>
        <th>categories</th>
        </tr>
        <tr>
        <td>{{ stock.stockcat }}</td>
      </tr>
    </tbody>
  </table>
  <div class="mt-4 d-flex justify-content-center">
    <a href="{{ path('app_stockc_edit', {'id': stock.id}) }}" class="btn btn-primary mx-3">Edit</a>
    {{ include('stockc/_delete_form.html.twig') }}
    <a href="{{ path('app_stockc_index') }}" class="btn btn-secondary mx-3">Back to List</a>
  </div>
</div>
{% endblock %}