{% extends 'base.html.twig' %}

{% block title %}Categoriesvehicules index{% endblock %}

{% block body %}
<style>
        table {
            margin: 50px auto;
            width: 80%;
            border-collapse: collapse;
        }
        th, td {
            padding: 10px;
            text-align: center;
            border: 1px solid #ddd;
        }
        th {
            color:blue ;
            
        }
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

     </style>

<div> <h1>Categoriesvehicules index</h1></div >
<div class="d-flex align-items-center justify-content-center" style="height: 550px;"> 

    
      
  <table class="table" style="
    
    margin-left: 410px;">

        <thead>
            <tr>
                <th>Id</th>
                <th>Typecatv</th>
                <th>actions</th>
              <th>  <form method="post" action="{{ path('app_categoriesvehicules_new') }} ">
    <button class="btn btn-success">create new</button>
</form></th>
                 
            </tr>
        </thead>
        <tbody>
        {% for categoriesvehicule in categoriesvehicules %}
            <tr>
                <td>{{ categoriesvehicule.id }}</td>
                <td>{{ categoriesvehicule.typecatv }}</td>
                <td>
<form method="post" action="{{ path('app_categoriesvehicules_delete', {'id': categoriesvehicule.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categoriesvehicule.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
                    <a href="{{ path('app_categoriesvehicules_edit', {'id': categoriesvehicule.id}) }}"><button class="btn btn-primary">Editer</button></a>
                     
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    </div>
{% endblock %}
